# Copy to .env and adjust as needed (optional; defaults work for local)
PORT=8082
DB_PATH=./data/app.db
# Required for login and protected routes (set a long random string in production)
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRY_HOURS=24
